# Notion Sync Placeholder

This file is a planning note for future Notion registry sync utilities.

Planned environment variables:

```text
NOTION_API_KEY=
NOTION_REGISTRY_DATABASE_ID=
NOTION_LOG_DATABASE_ID=
```

Planned future commands:

```bash
python scripts/foundry.py notion-pull
python scripts/foundry.py notion-push
```

Do not store Notion credentials in this repo. Use `.env` locally.
